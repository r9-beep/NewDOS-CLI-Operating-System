[package]
name = "disk-image-builder"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "disk-image-builder"
path = "src/main.rs"

[dependencies]
bootloader = { version = "0.11", features = ["bios", "uefi"] }
